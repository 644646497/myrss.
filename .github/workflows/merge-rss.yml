name: 抓取联合早报 RSS
on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: 抓取联合早报
        run: |
          curl -L -s "https://www.zaobao.com/rss.xml" > feed.xml
          echo "抓取完成"
      
      - name: 提交更新
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add feed.xml
          git diff --quiet && git diff --staged --quiet || git commit -m "更新联合早报"
          git push
